

// enclose '[' ... ']' as squareBrackets

// separator ',' in squareBrackets as list_literal
// separator ':' in squareBrackets count 1 as slicer

combine '#' hexDigits as hexNumber

define
    'if' '(' expression as condition')'
    block as ifTrueBlock 'else' block as ifFalseBlock
    as ifElse